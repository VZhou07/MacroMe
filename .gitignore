# MacroMe personal state: the plan (contains addresses), the order queue's
# lifecycle, what was eaten and spent, the saved end-of-day digests, and the
# lock that decides which process owns the cron.
macrome-config.json
macrome-queue-state.json
macrome-day-log.json
macrome-digests.json
macrome-scheduler.lock
macrome-*.tmp
macrome-*.json.*.tmp

node_modules/
.env
.venv/
__pycache__/
*.pyc
client_secret*.json
*token*.json
credentials.json
.env
.env.*
!.env.example
*.har
playwright-report/
test-results/
# Cursor local state stays private; share the MacroMe MCP config.
.cursor/*
!.cursor/mcp.json
.venv/
venv/
node_modules/

doordash-macro-agent/reports/

# Persistent digest email deduplication receipts
macrome-digests.json.*.email.json
macrome-scheduler.lock.*

# Raw USDA source CSVs (multi-GB, not for git; run join-nutrition-db to build from these)
doordash-macro-agent/data/fdc-raw/
